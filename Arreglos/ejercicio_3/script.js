/*Dado el arreglo [1, 2, false] Elimine el elemento de índice 2.*/

let arr = [1, 2, false];

// Eliminamos el elemento en el índice 2
arr.splice(2, 1);

// Mostramos el arreglo resultante
alert("El arreglo después de eliminar el índice 2 es: " + arr);
