/*Dado el siguiente arreglo let aux = [10, true, "k200", 20.7] , imprimir todos sus
elementos usando la operación de recorrido con foreach.*/

let aux = [10, true, "k200", 20.7];

// Recorremos el arreglo e imprimimos cada elemento
aux.forEach(function(elemento) {
  alert("Elemento: " + elemento);
});
