// Objeto inicial
let usuario = { password: "admin30905", rol: "admin", edad: 20 };

// Modificar los valores del objeto
usuario.password = "user_7833";
usuario.rol = "user";
usuario.edad = 34;

// Imprimir todo el objeto para verificar los cambios
console.log("Objeto después de los cambios:", usuario);
